spring:
  datasource:
    url: jdbc:mysql://localhost:3307/loopdfs
    username: root
    password: root

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
