spring:
  datasource:
    url: jdbc:mysql://localhost:3306/loopdfs
    username: root
    password: test16

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
server:
  port: 8081